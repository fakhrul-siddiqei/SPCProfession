<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spcprofession">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.showPosts.ShowDataAnalyst"></activity>
        <activity android:name=".ui.showPosts.ShowService" />
        <activity android:name=".ui.showPosts.ShowEngineer" />
        <activity android:name=".ui.showPosts.ShowDoctorProfile" />
        <activity android:name=".applyPost.HireProfessional" />
        <activity android:name=".ui.showPosts.ShowPosts" />
        <activity android:name=".ui.showPost.ShowPost" />
        <activity android:name=".ui.addProfession.AddProfession" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>